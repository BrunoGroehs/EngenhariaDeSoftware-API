{
  "name": "engenhariaDeSoftware-api",
  "version": "1.0.0",
  "description": "API para sistema de gerenciamento de tarefas",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "mocha test/**/*.test.js --timeout 10000",
    "test:watch": "mocha test/**/*.test.js --watch --timeout 10000",
    "test:coverage": "nyc mocha test/**/*.test.js --timeout 10000",
    "coverage:report": "nyc report --reporter=html"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "chai": "^5.2.0",
    "mocha": "^11.5.0",
    "nodemon": "^2.0.22",
    "nyc": "^17.1.0",
    "proxyquire": "^2.1.3",
    "sinon": "^20.0.0",
    "supertest": "^7.1.1"
  }
}
